import sqlite3

with sqlite3.connect('my_db.db') as db:
    cursor = db.cursor()
    #query = """ CREATE TABLE IF NOT EXISTS faces(id INTEGER, name TEXT, photo BLOB """
    #query0 = """ INSERT INTO faces (id, name, photo) VALUES(1, 'Ryan_Gosling', '') """ #создает запрос на создание в таблице faces строки с даными id = 1, name = Ryan_Gosling,  
    cursor.execute(query)
    cursor.execute(query0)
 

